This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Setup

Please run `npm install` to install all the dependencies.

Once installed, you will need to run the inital setup to setup the client ID and secret key for Sypht.

To setup run `npm run startup` and enter your client ID and Secret Key for Invoices.

Then run `npm start`